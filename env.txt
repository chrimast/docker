# ========================================
# OuonnkiTV 环境变量配置
# ========================================
# 使用说明：
# 1. 复制此文件为 .env：cp .env.example .env  (Linux/Mac) 或 copy .env.example .env (Windows)
# 2. 根据需要修改 .env 文件中的配置
# 3. 本地开发：pnpm dev
# 4. Docker 部署：docker-compose up -d --build
# ========================================

# ========================================
# 视频源配置
# ========================================
# 初始视频源配置（可选）
# 支持三种格式：单行JSON、多行JSON、或远程 JSON 文件 URL
# 留空表示不预配置任何视频源，用户可在应用中手动添加

# 方式1：单行 JSON 格式（推荐）
# VITE_INITIAL_VIDEO_SOURCES=[{"id":"source1","name":"示例源1","url":"https://api.example1.com","isEnabled":true}]

# 方式2：多个视频源（单行格式，用于 Docker Compose）
# VITE_INITIAL_VIDEO_SOURCES=[{"id":"source1","name":"源1","url":"https://api1.example.com","isEnabled":true},{"id":"source2","name":"源2","url":"https://api2.example.com","detailUrl":"https://detail.example.com","isEnabled":false}]

# 方式3：远程 JSON 文件 URL
# VITE_INITIAL_VIDEO_SOURCES=https://example.com/video-sources.json

# 留空（默认）
VITE_INITIAL_VIDEO_SOURCES=https://raw.githubusercontent.com/chrimast/ouonnkitv/main/tv-config.json

# 字段说明：
# - id: 源的唯一标识符（可选，会自动生成）
# - name: 源的显示名称（必需）
# - url: 源的API地址（必需）
# - detailUrl: 详情API地址（可选，默认使用url）
# - isEnabled: 是否启用（可选，默认为true）

# ========================================
# 分析配置
# ========================================
# 禁用分析跟踪（可选）
VITE_DISABLE_ANALYTICS=true

# ========================================
# Docker 构建配置（可选）
# ========================================
# 以下变量用于 Docker 镜像标签和元数据
# 通常在 CI/CD 流程中自动设置
BUILD_DATE=
VCS_REF=
VERSION=dev
